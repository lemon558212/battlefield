# GDD/18 — 待辦與建議（常駐清單）

> 2026-08-22 建立。**建立這份檔案的理由**：先前每一輪的「下一步建議」都只講在對話裡，
> 對話一結束就沒了。使用者下一次說「照你的建議全做」時，新開的 session 把倉庫 clone 下來，
> 找不到任何一份建議清單——commit 訊息、GDD、logs 都沒有。
>
> 從今以後：**任何「之後該做什麼」一律寫進本檔並 commit**。對話會消失，倉庫不會。

---

## 0. 怎麼在遠端容器裡做完整驗收（2026-08-22 實測可行）

先前的認知是「遠端 session 沒有 Godot，只能改碼、不能驗收」。實測不是這樣：

```bash
# 1) 下載與專案完全同版的引擎（版本要對，日誌開頭那一行就是版本）
curl -sSL -o godot.zip \
  https://github.com/godotengine/godot/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
unzip -q godot.zip && chmod +x Godot_v4.7.1-stable_linux.x86_64

# 2) 首次匯入資產（442MB、約 20 分鐘，只需一次）
./Godot_v4.7.1-stable_linux.x86_64 --headless --path godot --import

# 3) 跑驗收台。★ 不可以用 --headless：
#    headless 的 DisplayServer 下合成滑鼠事件不會按到按鈕（實測卡在「FAIL 無章節按鈕」），
#    而且視窗變成 1280×1280。要用 xvfb ＋ opengl3。
xvfb-run -a -s "-screen 0 1280x720x24" ./Godot_v4.7.1-stable_linux.x86_64 \
  --path godot --rendering-driver opengl3 -- locochk
```

**哪些台跑得動、哪些跑不動（2026-08-22 實測）**

| 台 | 本容器 | 說明 |
|----|--------|------|
| `-- locochk` | ✅ 約 2.5 分鐘 | 動作系統 19 項，實測 FAILS=0 |
| `-- aichk` | ✅ 約 2 分鐘 | AI 情報 15 項，實測 FAILS=0 |
| `-- play ch01` | ⚠ 前半段可用 | 操作類 8 項全 OK（切換單位／姿勢鍵／停下不原地跑步／鏡頭不抖／持槍手位置），但走到「走路」那段以 0.5m 一筆的速度前進，2 fps 下跑完一場要好幾小時 |
| `-- walk ch01` | ❌ | 130 個取樣點 × 7000 次檢查，30 分鐘連第一段都沒走完 |
| `-- stress` / `-- selftest` | ❌ 未試 | 同理，截圖與長程走查在軟體渲染下不實際 |

**結論**：短程、數值型的驗收台在遠端容器完全可用；長程走查與截圖類必須在本機跑。
改動如果會影響移動、地形或平衡，**本機那一輪不能省**。

⚠ `timeout N` 只殺得掉 `xvfb-run`，殺不掉底下的 Godot——要停請用
`pkill -f "[-]- play ch01"` 這種對完整命令列的比對。

**已知代價**：軟體渲染（llvmpipe）只有 2~7 fps。數值型斷言照樣成立，但
**任何「單幀多少」的門檻都會假性 FAIL**——那不是程式壞了，是門檻寫錯了（見 §1 第一條）。
截圖也會比本機難看，所以**不要把這個環境跑出來的 qa/*.png 提交進版控**。

---

## 1. 本輪（2026-08-22）已完成

0. **這個容器其實跑得動驗收台**（見 §0）——先前「遠端只能改碼不能驗收」的認知是錯的。
1. **locochk 的轉向斷言改成幀率無關**：原本驗「單幀最大轉 < 22°」，那是角速度 × 幀時，
   在慢機器上必 FAIL。改驗角速度（度/秒），上限對照 `LocomotionProfile.turn_rate_fast`。
2. **AI 全知關掉（GDD/05 §5 補回）**：`_ai_plan` 原本把所有活著的敵方單位當候選，
   迷霧、掩體、草叢、視野扇形對 AI 全都不算數。改用 `_known_foes` / `_nearest_known`，
   與玩家共用同一支 `_spots`。
3. **AI 聽覺成為情報（GDD/15 F6）**：`_hear_shot` 把「開槍當下的位置」記進聽者，
   20 秒有效；看不到人但記得聲音 → 「循槍聲查看」。
4. **新驗收台 `-- aichk`**：把上面三件事逐條量出來（見該台的輸出）。
5. **GDD/15 新增 J 類「情報與認知」**，把「誰能知道什麼」當成獨立一類物理來管。
6. **J6 移動噪音**：移動中每 0.4s 登記噪音，半徑站姿 120px／蹲行 55px／載具 260px／
   匍匐 0（槍聲是 460px）。量到：站姿移動 72px 外的敵人聽得到、匍匐同距離聽不到。
   「安靜接近」第一次有反面——先前站著全速跑跟蹲著挪對 AI 是同一件事。
7. **J5 陣亡地點是情報**：`_battle_marks` 記下每個死亡位置，同陣營看得到那個位置的人
   會提高警覺，每筆只驚動一次（不然一具屍體會把 AI 釘在原地）。
   量到：誤差 0.00px、重複登記 0 次。
   ⚠ 記的是「這裡死過人」，不是躺著的屍體——`Unit.die()` 淡出後節點就移除了，
   可見殘留是美術資產的事（同 G3 車輛殘骸）。
8. **AI 難度分級接上情報**：`data/difficulty.json` 每章新增 `hearMemory`（記憶秒數）
   與 `relay`（同袍通報半徑 px）。ch1~2＝8s／120px（幾乎不通報、忘得快），
   ch13~15＝26s／9999px（全隊即時共享）。高難度＝知道得更多更久，不是血更厚。
   量到：ch01 記憶 4s 還記得、9s 就忘；同袍看得到我但隔 380px 的敵人已知數＝0。
9. `aichk` 擴充到 **22 條斷言，FAILS=0**。
   ★ 過程中踩到一次測試自己的坑：第一版把陣亡痕跡放在「敵人與我之間」，
   我方單位的身體擋住了那條視線（人是固體，`_shot_clear` 連人一起算），
   量到 `sight_clear=false`——不是 J5 壞了，是測試自己站在中間。

---

## 2. 建議下一步（按投資報酬率排）

### 第一梯：現在就能做，且能量得出來
- **警戒射擊要先「看得到」**：`_intercept_tick` 目前只驗彈道通不通（`_shot_clear_units`）＋
  射程＋不在草叢，**不看視野扇形**——背後有人跑過去照樣自動開火。這對雙方對稱，
  所以不是作弊，但違反鐵律 0②與 I1（人看不到背後）。改法是加一道 `_spots(u, m)`。
  ⚠ 這條會動到平衡與既有 `selftest` 的 alertchk，本輪**刻意沒做**：
  遠端容器 2 fps 跑不動 selftest 那台截圖馬拉松，改動沒辦法在同一輪驗完。
  要做請在本機跑 `-- selftest` 與兩章 `-- play` 對照勝負與迎擊次數。

### 第二梯：需要使用者拍板
- **G1 可破壞環境**：工事與中景為了 draw call 合併成單一網格，逐件摧毀要先拆合併，
  是一次真正的重構。要不要做、做到什麼程度，需要先定範圍。
- **H2 涉水表現**：水裡照播走路動畫、沒有水花。屬於動畫與特效資產缺口。
- **跑步片段**：`run/sprint` 仍有滑步（`Jog_Fwd` 重定向到 hr_ 骨架後腿幾乎不掃），
  真正的解法是換一份可用的跑步動畫資產，不是再調參數。

### 第三梯：資產產線（需要 64GB 那台，見 GDD/17 §六）
- 八機種載具成品模型、九名角色的動畫烘焙。這條線與本容器無關，別在這裡空轉。

---

## 2b. 本輪沒能在容器裡驗完、需要在本機補跑的

- `-- play ch01` 後半（開火、擊殺、勝利判定）與 `-- stress`：**AI 情報改動直接影響敵方行為**，
  行為面的回歸只能在本機跑。要看的是：敵方階段有沒有出現「循槍聲查看」、
  整場長度有沒有暴增（AI 找不到人就推主堡，理論上會更快而不是更慢）、勝負是否仍合理。
- `-- walk` 全 15 章：這輪完全沒跑（見 §0）。本輪沒有動移動與地形，風險低，但仍應補。

## 3. 維護規則

- 做完一項就從 §2 移到 §1，並寫上「量到什麼數字」。
- 新的建議一律進 §2，不要只講在對話裡。
- 與 GDD/15（物理稽核）分工：**15 是「哪裡不真實」的帳，18 是「接下來做什麼」的帳**。
